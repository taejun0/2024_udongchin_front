name: CD for React App

on:
  workflow_run:
    workflows: ["CI for React App"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify artifacts exist (Debug)
        run: |
          echo "Listing artifacts for run ID: ${{ github.event.workflow_run.id }}"
          curl -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
          https://api.github.com/repos/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }}/artifacts

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build
          path: ./downloaded-build

      - name: Verify downloaded artifact (Debug)
        run: ls -R ./downloaded-build

      - name: Install Vercel CLI
        run: |
          npm cache clean --force
          npm install -g vercel@latest

      - name: Verify VERCEL_TOKEN (Debug)
        run: |
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ]; then
            echo "Error: VERCEL_TOKEN is not set." >&2
            exit 1
          else
            echo "VERCEL_TOKEN is set correctly."
          fi

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          vercel --prod --confirm --cwd ./downloaded-build
